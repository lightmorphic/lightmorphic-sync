# Changelog

## Unreleased

- Initial scaffold: `sync-api` (zero-knowledge sync, SQLite) and
  `webstore-proxy` (Chrome Web Store reverse proxy), Docker images built
  to GHCR on push/weekly, self-host `docker-compose.yml`.
